---
export interface Props {
    rank: string;
    sizes?: string;
    margins?: string;
}

const {rank, sizes='h-8', margins=''} = Astro.props

const rank_icon='/img/'+ (rank>=1200 ? 'master' : rank) +'_icon.png';
---

<img class={`${sizes} ${margins}`} src={rank_icon} />